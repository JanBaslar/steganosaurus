import 'package:flutter/material.dart';

/// Defines all style constants used in app.
class Styles {
  /// Seed color used for themes.
  static const MaterialColor seedColor = Colors.lightGreen;

  /// Widget marging from screen adges.
  static const double allMargin = 30.0;

  /// Button outline thickness for OutlineButtons and TextFields.
  static const double borderThickness = 2.0;

  /// Form space between relative elements.
  static const double smallGap = 10.0;

  /// Form space between separate elements.
  static const double bigGap = 30.0;

  /// Corner radius for app elements.
  static const double borderRadius = 10.0;

  /// Text size for important elements.
  static const double importantTextSize = 18.0;
}
